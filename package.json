{
  "name": "my-ui-playground",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "storybook": "pnpm --filter web storybook",
    "build:ui-core": "pnpm --filter ui-core build",
    "dev:server": "pnpm --filter mock-server dev",
    "start": "concurrently \"pnpm dev:web\" \"pnpm dev:server\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
